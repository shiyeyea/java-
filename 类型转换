 public static void main(String[] args) {
        //类型之间的转化： 父——>子

        //高——————>低
        farther obj=new son();
        //obj将这个对象转换为son类型，我们就可以使obj类型的方法了!
        ((son) obj).go();//强制转换
    /*
    父类引用指向子类的对象
    把了类转换为父类，向上转型
    :把父类转换为子类，向下转型： 强制转换
    方使方法的调用，减少重复的代码!简介
    */
    }
